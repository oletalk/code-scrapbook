# your local system info goes here
# fill in your values and 
# save it to a file called .env in the same directory
# Password for the database user
export DBPASS=secret
# Temporary directory for files downloaded from SFTP store
export CACHE_DIR=/var/tmp/files
# SFTP server containing your tunes...
export SFTP_HOST=192.168.1.3
# SFTP user with access to them
export SFTP_USER=barney
# password for the user
export SFTP_PASSWORD=il0veb33r
