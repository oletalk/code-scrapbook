require 'rake'
require 'rspec/core/rake_task'

RSpec::Core::RakeTask.new(:test) do |t|
    t.pattern = Dir.glob('spec/**/*.spec')
end

RSpec::Core::RakeTask.new(:itest) do |t|
    t.pattern = Dir.glob('rack_test/*.spec')
end
task :default => :test
